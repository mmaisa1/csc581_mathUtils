pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                sh 'python -m py_compile MathUtils.py test_MathUtils.py' 
                stash(name: 'compiled-results', includes: '*.py*') 
            }
        }
    }
}
